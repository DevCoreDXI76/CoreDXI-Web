/**
 * page.tsx — 홈페이지 (/ 경로)
 *
 * 이 파일은 CoreDXI 홈페이지의 메인 페이지입니다.
 * 각 섹션 컴포넌트를 여기서 불러와 조합합니다.
 *
 * [홍보팀 안내]
 * 이 파일은 어떤 컴포넌트를 어떤 순서로 보여줄지만 결정합니다.
 * 실제 텍스트/이미지 수정은 각 컴포넌트 파일(src/components/)에서 하세요.
 */

import { Hero } from "@/components/Hero";

/**
 * 홈페이지 컴포넌트
 * 홈페이지를 구성하는 섹션들을 순서대로 나열합니다.
 */
export default function HomePage() {
  return (
    <main>
      {/* 히어로 섹션: 첫 화면에 표시되는 메인 소개 영역 */}
      <Hero />

      {/*
       * [홍보팀] 아래에 새 섹션을 추가할 때는 개발팀에 문의하세요.
       * 예: 기능 소개 섹션, 고객사 로고, 도입 사례 등
       */}
    </main>
  );
}
