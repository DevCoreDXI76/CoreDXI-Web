/**
 * page.tsx — 홈페이지 (/ 경로)
 *
 * CoreDXI 홈페이지의 메인 페이지입니다.
 * 각 섹션 컴포넌트를 순서대로 불러와 조합합니다.
 *
 * ── 변경 이력 ──────────────────────────────────────────────────────
 * v0.2  2026-05-14  Header 컴포넌트 추가
 *       - <Header /> 임포트 및 <main> 외부 배치 (Semantic HTML 구조)
 *       - <> fragment 사용으로 불필요한 div 래퍼 제거
 * v0.1  2026-05-14  최초 생성
 *       - Next.js 보일러플레이트 제거, <Hero /> 단독 렌더링
 * ────────────────────────────────────────────────────────────────────
 *
 * [홍보팀 안내]
 * 이 파일은 어떤 컴포넌트를 어떤 순서로 보여줄지 결정합니다.
 * 실제 텍스트/이미지 수정은 각 컴포넌트 파일(src/components/)에서 하세요.
 */

import { Header } from "@/components/Header";
import { Hero } from "@/components/Hero";

/**
 * 홈페이지 컴포넌트
 * SEO를 위해 <header>와 <main> Semantic 태그를 명확히 분리합니다.
 */
export default function HomePage() {
  return (
    <>
      {/* 모든 페이지 상단에 고정되는 네비게이션 헤더 */}
      <Header />

      <main>
        {/* 첫 화면: 메인 소개 히어로 섹션 */}
        <Hero />

        {/*
         * [홍보팀] 아래에 새 섹션을 추가할 때는 개발팀에 문의하세요.
         * 예: 기능 소개, 고객사 로고, 도입 사례, 요금제 등
         */}
      </main>
    </>
  );
}
